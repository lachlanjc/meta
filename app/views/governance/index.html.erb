<% title 'Governance', @product.name %>
<% activate_nav! :people %>
<% activate_nav! :people_governance %>

<%= render 'people/nav' %>

<% if staff? %>
  <div class="sheet-body">
    <div class="row">
      <div class="md-col-12 sm-col-12">
        <%= react_component('Governance', product: ProductGovernanceSerializer.new(@product).as_json) %>
      </div>
    </div>
  </div>
<% end %>
